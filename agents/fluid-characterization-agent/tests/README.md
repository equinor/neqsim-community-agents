# Fluid Characterization Agent Tests

Repository-level tests validate that this agent includes the required files,
metadata fields, documentation sections, examples, and prompts, and that every
`required_skills` entry resolves to a published community skill.

Future agent-specific tests may validate the split-factor calibration workflow
structure, expected residual reporting, and allocation-weight consistency.
