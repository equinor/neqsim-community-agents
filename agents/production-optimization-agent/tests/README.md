# Production Optimization Agent Tests

Repository-level tests validate that this agent includes required files, metadata
fields, documentation sections, examples, and prompts.

Future agent-specific tests may validate decision-variable framing, setpoint
screening categories, and operating-window guideline checks.
