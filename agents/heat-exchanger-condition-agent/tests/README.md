# Heat Exchanger Condition Agent Tests

Repository-level tests validate that this agent includes required files, metadata
fields, documentation sections, examples, and prompts.

Future agent-specific tests may validate that the two-sided flow normalisation is
used in preference to the one-sided form, that the maldistribution rule-out runs
before a fouling verdict is issued, and that the capacity statement names its
binding constraint.
