# Control Authority Agent Tests

Repository-level tests validate that this agent includes required files, metadata
fields, documentation sections, examples, and prompts.

Future agent-specific tests may validate that both output stops are counted, that
an authority-loss verdict is never issued without a disturbance measure for every
compared period, and that the finding states what consumed the margin or says
explicitly that it is not yet known.
