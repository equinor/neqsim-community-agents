# Templates

This directory contains reusable templates for creating new NeqSim Community Agents.

Start from [agent-template](agent-template/) when adding a new agent. Copy the directory to `agents/<agent-name>/`, then update all placeholders, examples, prompts, and tests.