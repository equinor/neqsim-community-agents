# Tests

This directory contains repository-level tests for structure and metadata.

Run with:

```powershell
python -m unittest discover -s tests
```

The initial tests use only the Python standard library.